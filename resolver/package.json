{
  "name": "@unidpp/resolver-worker",
  "private": true,
  "version": "0.1.0",
  "description": "UniDPP resolver service: KV-backed RFC 9264 linksets with per-context routing (EN 18222 / GB/T 33993 / UNTP renders), as-of stamps, and dark-identity no-information 404s. .",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/vitest-plugin": "^1.1.4",
    "@types/node": "^22.20.1",
    "vitest": "^4.1.0",
    "wrangler": "^4.129.0"
  }
}
